spring:
  datasource:
    url: jdbc:mariadb://localhost:3307/testjpa
    username: root
    password: mariadb
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:

        format_sql: true
logging.level:
  org.hibernate.SQL: debug

